public class Dummy {
    public static void main(String[] args) {
        Master master = new Master(Integer.parseInt(args[0])); //initializes the master
        //gets the gpx files

        /* sends the gpx files via tcp
        *  we need to create a socket and flush the files  */
    }
}